/**
 * Type stub for the plain-JS `logger.js` so TypeScript code in channel
 * packages can import it without `noImplicitAny` errors.
 */

import type { Logger } from 'pino';

declare const logger: Logger;
export { logger };